server:
  port: ${PORT}

spring:
  application:
    name: apigateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      routes:
        - id: gastroreview-route
          uri: lb://gastroreview
          predicates:
            - Path=/gastroreview/**
          filters:
            - StripPrefix=1

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: https://eurekaserver-lzph.onrender.com/eureka/

  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.value}
